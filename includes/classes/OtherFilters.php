<?php
/**
 * A place to put filters that don't belong elsewhere and can't be grouped in such numbers that warrant their own
 * classes.
 *
 * @package Orbit
 */

namespace Eighteen73\Orbit;

/**
 * A place to put filters that don't belong elsewhere and can't be grouped in such numbers that warrant their own
 * classes
 */
class OtherFilters {
	use Singleton;

	/**
	 * Run on init
	 *
	 * @return void
	 */
	public function setup() {}
}
